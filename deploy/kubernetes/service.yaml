apiVersion: v1
kind: Service
metadata:
  name: juggluco-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: juggluco-backend
  ports:
    - name: http
      port: 8765
      targetPort: http
